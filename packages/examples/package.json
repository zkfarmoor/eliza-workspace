{
  "name": "@workspace/examples",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "lifi-sdk"
  ],
  "scripts": {
    "build": "turbo run build --filter=\"@workspace/example-*\"",
    "clean": "turbo run clean --filter=\"@workspace/example-*\"",
    "example": "turbo run example --filter=\"@workspace/example-*\""
  },
  "devDependencies": {
    "@types/node": "^22.8.5",
    "typescript": "^5.6.3",
    "tsx": "^4.19.2"
  }
}
